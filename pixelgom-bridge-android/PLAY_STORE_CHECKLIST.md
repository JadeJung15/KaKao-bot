# Play Store 체크리스트

## 앱 정보

| 항목 | 값 |
|---|---|
| 앱 이름 | 픽셀곰 브릿지 |
| 기본 언어 | 한국어 |
| 패키지명 | com.pixgom.bridge |
| 카테고리 | 도구 |
| 배포 대상 | 픽셀곰 운영 관리자 |
| 현재 Play 등록 | 1.0.21 (22) |
| 다음 준비 AAB | 1.0.22 (23) |

## 2026-05-25 비공개 테스트 기준

| 항목 | 상태 |
|---|---|
| 테스터 CSV | `픽셀곰 내부 테스트` 목록 29명 반영 |
| AAB | `1.0.20 (21)` 내부 테스트 및 비공개 테스트 Alpha 트랙 게시 완료 |
| 스토어 등록정보 | 설명, 앱 아이콘, 그래픽 이미지, 휴대전화/태블릿 스크린샷 저장 완료 |
| 개인정보처리방침 | `https://pixgom.com/privacy` 저장 완료 |
| 앱 액세스 | 제한 없이 검토 가능으로 저장 |
| 광고 | 광고 없음으로 저장 |
| 정부 앱 | 정부 앱 아님으로 저장 |
| 카테고리/연락처 | 도구, `jadejung15@gmail.com`, `https://pixgom.com` 등록 |
| 콘텐츠 등급 | IARC 설문 저장 완료, 국내 등급 `3세 이상` |
| 타겟층 | `만 18세 이상` 저장 완료 |
| 데이터 보안 | 이름, 사용자 ID, 기타 인앱 메시지, 앱 상호작용, 기타 작업 수집으로 저장 |
| 광고 ID | 광고 ID 사용 안 함으로 저장 |
| 테스트 트랙 제출 | 내부 테스트 활성, 비공개 테스트 Alpha 트랙 활성, 12명 테스터 참여 선택 완료 |

## 2026-05-26 Play Console 확인

| 항목 | 상태 |
|---|---|
| 앱 | 픽셀곰 브릿지 / `com.pixgom.bridge` |
| 내부 테스트 | `22 (1.0.21)` 업로드 완료 |
| 비공개 테스트 | `비공개 테스트 - Alpha` 활성 |
| 게시 버전 | `22 (1.0.21)` |
| 프로덕션 상태 | 비활성 |
| 프로덕션 액세스 조건 | 비공개 테스트 버전 게시 완료, 12명 이상 참여 선택 완료, 14일 이상 운영 대기 중 |
| 현재 경과 | 12명 테스터가 1일 동안 참여 선택 |

## 2026-05-26 다음 내부 테스트 준비

| 항목 | 상태 |
|---|---|
| 준비 버전 | `1.0.22 (23)` |
| 업로드 대상 | 내부 테스트 트랙 |
| 릴리스 노트 | 일반방 연결코드로 연결된 게임방까지 자동 등록, 게임방 무응답 개선 |
| 변경 범위 | Android 다중 방 연결코드 파싱, 서버 `rooms[]` 응답 호환 |

## 다음 AAB 생성/검증

```powershell
npm.cmd run android:bundle
```

출력에서 아래 값을 확인합니다.

| 항목 | 기준 |
|---|---|
| track | `internal-testing` |
| versionName | `1.0.22` |
| versionCode | `23` |
| aabPath | `pixelgom-bridge-android/app/build/outputs/bundle/release/app-release.aab` |
| sha256 | 비어 있지 않은 64자리 해시 |

현재 Play Console 내부 테스트와 비공개 테스트 Alpha에 등록된 최신 등록 버전은 `1.0.21 (22)`이며, 이번 내부 테스트 업로드 대상은 `1.0.22 (23)`입니다.

이번 Play Console 등록 대상은 **내부 테스트(Internal testing)** 트랙입니다. 비공개 테스트(Closed testing / Alpha)와 프로덕션 트랙에는 별도 지시 전까지 업로드하지 않습니다.

남은 Play Console 항목:

1. 12명 이상 테스터 기준으로 14일 이상 비공개 테스트 운영.
2. 14일 요건 충족 후 프로덕션 액세스 신청 설문 진행.
3. 프로덕션 액세스 승인 후 관리형 게시 상태에서 실제 프로덕션 게시 여부를 별도 결정.

## 짧은 설명

픽셀곰 운영방 알림을 봇 서버와 로컬 JS 자동응답으로 연결하는 전용 브릿지 앱

## 전체 설명 초안

픽셀곰 브릿지는 등록된 카카오 오픈채팅 운영방 알림을 픽셀곰 운영봇 서버와 로컬 JS 자동응답으로 연결하는 관리자용 브릿지 앱입니다.

앱은 사용자가 직접 허용한 알림 접근 권한을 통해 카카오톡 알림의 방 이름, 발신자명, 메시지 내용을 읽고, 등록된 운영방으로 확인된 알림만 서버로 전송합니다. 서버에서 응답이 반환되면 카카오 알림의 답장 기능을 통해 운영봇 응답을 보낼 수 있습니다.

서버 응답이 없는 경우에는 사용자가 앱 안에 저장한 JS 자동응답 스크립트가 보조 응답을 만들 수 있습니다. 이 앱은 일반 채팅 수집 앱이 아니며, 설정된 픽셀곰 운영방 외 알림은 전송하지 않습니다.

## 심사 메모 초안

이 앱은 카카오톡 오픈채팅 운영방의 관리자용 봇 브릿지입니다. 핵심 기능은 등록된 운영방 알림을 서버와 로컬 JS 자동응답으로 전달하고 생성된 응답을 알림 답장 액션으로 보내는 것입니다. 이를 위해 Android Notification Listener 권한이 필요합니다. 앱은 접근성 권한과 화면 감지를 사용하지 않으며, 등록된 방 이름과 일치하지 않는 알림은 서버로 전송하지 않습니다.

테스트 순서:
1. 앱 실행
2. 알림 접근 권한 허용
3. 구매자 가이드의 앱 연결코드로 자동 설정 실행
4. 앱의 서버 테스트 전송 실행
5. 앱의 JS 테스트 실행
6. 등록된 카카오 운영방에서 `/상태`, `/브릿지`, `/js상태` 입력
7. 앱의 `테스트 체크리스트`에서 `/구독상태`, `/포인트`, `/출석`, `/게임`, `/주사위` 순서 확인
8. 서버 또는 JS 응답이 카카오 알림 답장으로 전송되는지 확인

## Data Safety 초안

수집/전송 항목:

- 앱 활동: 앱 전송 로그
- 메시지: 등록된 운영방의 카카오 알림 메시지 내용
- 개인 정보: 카카오 알림에 표시되는 닉네임

사용 목적:

- 앱 기능 제공
- 운영방 봇 자동 응답
- 장애 진단
- 사용자가 직접 저장한 로컬 JS 자동응답 실행

공유:

- 픽셀곰 운영봇 서버로 전송
- 제3자 광고/마케팅 공유 없음

보안:

- HTTPS 전송
- 등록된 방 이름과 roomId 기준 필터링
